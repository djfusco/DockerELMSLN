## Instructions

```
git clone http://github.com/elmsln/elmsln.git
docker-compose up
```

Visit `http://localhost`

Use database creds:
- database: `root`
- user: `root`
- password: `root`
- DB host: `db`
